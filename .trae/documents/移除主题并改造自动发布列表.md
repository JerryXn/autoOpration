## 变更概览
- 删除“主题”字段：Excel 模板、解析、数据库、后端、前端一并移除。
- 自动发布列表改为：标题、文案（截断+悬停显示全文）、封面（1张）、配图（全部缩略图）、平台、状态、创建时间。
- 保持布局稳定：统一 Pane 高度与滚动，表格固定布局。

## 数据库与后端
- 表结构：执行 `ALTER TABLE parsed_contents DROP COLUMN subject`。
- 保存入库：后端写入时去掉 `subject`，其余字段不变（`title/content/cover_refs/image_refs/...`）。
- 列表接口：分页查询返回 `coverThumbUrl` 与 `imageThumbUrls[]`，去掉 `subject` 字段；维持筛选 `platformCode`、`page/pageSize`。
- 缩略图 URL：仍用 `pathToFileURL(abs).href` 生成 `file://` 可见链接。

## Excel 模板与解析
- 模板生成：`genTemplate()` 表头改为 `['标题','文案','封面','配图']`，示例同步修改。
- 解析：`parseExcelFile()` 去掉对 `主题/subject` 的读取；返回行仅含 `title/content/cover/images`。
- 预览表：删除“主题”列，保留匹配状态展示。

## 前端列表改造
- 自动发布 Tab 表格列：
  1. 标题：文本列
  2. 文案：前端截断（如 36 字符），`title` 或 tooltip 悬停显示全文
  3. 封面：固定 60×80 缩略图
  4. 配图：全部缩略图 40×40 横向排列（超出自动换行或显示“+N”）
  5. 平台、状态、创建时间
- 表格布局：`table-layout: fixed`，列宽与省略号样式，避免内容撑破。
- Pane 高度与滚动：统一 `min-height + overflow:auto`，Tab 位置不变。

## 验证
- 导入 Excel（无“主题”列）→ 预览与导入成功。
- 新记录查询列表显示正确列；文案悬停完整展示；封面与全部配图可见。
- Tab 切换位置稳定；分页、滚动正常。

请确认，我将按以上步骤进行实现并联调。