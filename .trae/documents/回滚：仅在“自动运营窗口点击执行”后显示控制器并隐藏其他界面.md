## 目标
- 恢复原始体验：进入“自动运营”页面时显示配置窗口；只有当你点击“执行”后，才出现 AutoOp 控制浮窗，并隐藏其他界面。

## 实施要点
- 渲染层（src/views/autoop.js）
  - 移除在 mount 阶段的 `renderFloater()` 调用，初始不显示浮窗。
  - 在“执行成功”回调里先调用 `renderFloater()`，再添加 `controller-only` 类并隐藏 `#autoop-card`，只保留浮窗（居中显示已就位）。
  - 在“停止”回调里移除 `controller-only`，隐藏浮窗并重新显示 `#autoop-card`。
- 样式与容器保持现状（已实现 controller-only 与居中显示）。

## 验证
- 进入“自动运营”页面：看得到原始执行配置窗口；浮窗不出现。
- 点击“执行”：仅浮窗居中出现，其他界面隐藏。
- 点击“停止”：恢复原界面显示，浮窗隐藏。

确认后我将立即回滚并实现上述逻辑。