**修改计划**

1.  **前端页面 (`src/views/autoop.js`)**
    *   **重写“执行”按钮逻辑**：
        *   移除原有的复杂参数组装和后端调用逻辑。
        *   实现点击后直接弹出浏览器原生警告框 (`alert('正在执行')`)。
        *   将状态文本更新为“正在执行”。
    *   **重写“停止”按钮逻辑**：
        *   移除原有的后端停止调用。
        *   实现点击后仅重置界面状态（如将状态文本改回“就绪”或“已停止”），不再尝试终止任何后台进程。

2.  **后端主进程 (`src/main.js`)**
    *   **清理 `start-auto-op` 接口**：
        *   彻底删除所有与 `auto_opration_python` 目录、Python 环境检查、参数拼接及 `spawn` 进程调用相关的代码。
        *   改为直接返回 `{ ok: true }` 的空实现，防止前端报错。
    *   **清理 `stop-auto-op` 接口**：
        *   删除终止 Python 进程的逻辑。
        *   改为直接返回 `{ ok: true }`。

此方案将彻底切断对已删除 Python 目录的依赖，并按要求将功能简化为纯前端的弹框交互。